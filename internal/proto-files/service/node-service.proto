syntax = "proto3";

package service;

service NodeService {
    rpc GetNodeVersion(Request) returns (Response) {}
}

message Request { 
    string body = 1;
}
message Response {
    string body = 1;
}

